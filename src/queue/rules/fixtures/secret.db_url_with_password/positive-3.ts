// `postgresql://` is the spelling PostgreSQL's own docs and Prisma use, so
// it is what a real DATABASE_URL almost always says.
const dbUrl = 'postgresql://admin:Sup3rSecret99@db.example.com:5432/prod';
