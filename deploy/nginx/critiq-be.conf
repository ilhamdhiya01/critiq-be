# Reference template — copy manually to /etc/nginx/sites-available/ on the
# VPS (not run as a container: this Nginx also proxies to the FE, which is
# deployed separately from this repo's docker-compose.yml).
#
# TLS is terminated at Cloudflare (orange-cloud proxy), so this only needs
# to listen on plain HTTP — no certbot/Let's Encrypt setup on the VPS.
# Make sure the Cloudflare DNS record for critiq-dev.web.id is proxied
# (orange cloud, not grey/DNS-only) and SSL/TLS mode is "Flexible" — origin
# stays plain HTTP. Upgrading to "Full (strict)" later would need a
# Cloudflare Origin CA certificate on this VPS; not needed for now.

server {
    listen 80;
    server_name critiq-dev.web.id;

    location /api/v1 {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # Cloudflare -> this VPS is plain HTTP even though the original
        # client request was HTTPS to Cloudflare — without this, anything
        # in the app inspecting req.protocol would wrongly assume the whole
        # chain is HTTP.
        proxy_set_header X-Forwarded-Proto https;
    }

    # FE (critiq-fe) is deployed separately from this compose setup — point
    # this at wherever that ends up running.
    location / {
        proxy_pass http://127.0.0.1:<FE_PORT_PLACEHOLDER>; # TODO: set to the FE's actual port/host
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
